const firstName ="zakaria";
const secondName="Iqbal";

//const fullname1 =firstName + " "+secondName +" is a good boy";

// using `` as a template you can use it in this format.

const fullname2 =`${firstName} ${secondName} is a good boy`;

console.log(fullname2);

const multiple =`hi this a Another example of template at 
yes it can use multiple line within this 
Another One `

console.log(multiple);